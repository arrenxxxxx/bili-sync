name: Build Main Binary

on:
  push:
    branches:
      - main

jobs:
  build-binary:
    if: false  # disabled: main 分支构建已由 pr-check.yaml 中的 test 覆盖，无需单独构建二进制
    # if: ${{ !startsWith(github.ref, 'refs/tags/') }}
    uses: ./.github/workflows/build-binary.yaml
